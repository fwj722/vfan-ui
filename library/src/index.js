import { browser } from "./browser"
import date from "./date"
import debounce from "./debounce"
import throttle from "./throttle"
import { exportExcel } from "./exportExcel"
import { unique, setUnique, concatArr, sort, arrayObjSort } from "./array"
import { jsonDeepClone, objDeepClone } from "./deepCopy"
import { trim } from "./string"

export default {
  browser,
  date,
  debounce,
  throttle,
  exportExcel,
  unique, 
  setUnique, 
  concatArr, 
  sort, 
  arrayObjSort, 
  jsonDeepClone, 
  objDeepClone,
  trim
}